import spacy

def(token_words, taggedwords, questions):

    #fill in the blanks
    for n in token_words:

        if(n.tag == 'NNP'_):
            for i in token.ents:

                if(n.ents == 'PERSON'):
                    if(n.dep_ == 'compound'):

                        questions.append(str(n, n.head).replace(str(n, n.head,"_")))






